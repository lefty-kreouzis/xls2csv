<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    ​
    <springProperty scope="context" name="springAppName" source="spring.application.name"/>
    <!-- Example for logging into the build folder of your project -->
    <property name="LOG_FILE" value="xls2csv"/>​

    <!-- You can override this to have a custom pattern -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSSZ}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>



   <appender name="FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>log/${LOG_FILE}.log</file>
		 <!--<file>D:\log\audit-service.log</file> -->
		<rollingPolicy
			class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<!-- daily rollover -->
			<fileNamePattern>log/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern> 
			<!--<fileNamePattern>D:\log\audit-service.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>--> 

			<!-- keep 30 days' worth of history capped at 3GB total size -->
       		<maxFileSize>100MB</maxFileSize>    
			<maxHistory>30</maxHistory>
			<totalSizeCap>3GB</totalSizeCap>
		</rollingPolicy>

		<encoder>
			<pattern>${CONSOLE_LOG_PATTERN}</pattern>
		</encoder>
	</appender>
    ​
<statusListener class="ch.qos.logback.core.status.NopStatusListener" />

  <logger name="net.bull.javamelody" level="WARN" /> ​
  <logger name="org.springframework.security" level="WARN" /> ​
  <logger name="ch.qos.logback" level="WARN" /> ​
  <root level="INFO">
		<appender-ref ref="FILE" />
    </root>

</configuration>